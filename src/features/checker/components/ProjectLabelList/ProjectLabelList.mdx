import { Meta, Title, Primary, Stories, Controls, Story } from "@storybook/blocks";
import * as ProjectLabelListStories from "./ProjectLabelList.stories";

<Meta of={ProjectLabelListStories}/>

# ProjectLabelList Component

The `ProjectLabelList` component displays a list of project-related labels such as addresses, social media links, and dates. It uses Tailwind CSS for styling and `tailwind-variants` for variant handling.

# Example

<Story of={ProjectLabelListStories.Default} />

# Controls

<Controls />
<Stories />
