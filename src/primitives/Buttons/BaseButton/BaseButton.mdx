import { Meta, Title, Primary, Stories, Controls, Story } from "@storybook/blocks";
import * as BaseButtonStories from "./BaseButton.stories";
import { BaseButton } from "./BaseButton";

# BaseButton

<Meta of={BaseButtonStories} />

The `BaseButton` component is a versatile button that supports various visual styles and sizes. It allows for the inclusion of icons, different states (enabled/disabled), and customizable variants, making it suitable for different use cases across your application.

## Example

### Default BaseButton

<Story of={BaseButtonStories.Default} />

## Controls

<Controls />

## Other Variations

<Stories />
