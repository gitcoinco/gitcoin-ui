import { Meta, Title, Story, Controls, Stories } from "@storybook/blocks";

import * as RoundTypeBadgeStories from "./RoundTypeBadge.stories";

# RoundTypeBadge

<Meta of={RoundTypeBadgeStories} />

The `RoundTypeBadge` component supports different types of rounds badges.

<Story of={RoundTypeBadgeStories.QuadraticFunding} />

<Controls />

### DirectGrants

The `DirectGrants` badge indicates rounds where funds are directly granted to recipients without intermediary processes.

<Stories />
